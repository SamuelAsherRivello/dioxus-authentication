# Web Package

Web app entrypoint, favicon, and web CSS for Dioxus Authentication. This crate renders the reusable `authentication` package directly.

Run from the repository root with:

```powershell
.\Scripts\Common\RunWeb.ps1
```
