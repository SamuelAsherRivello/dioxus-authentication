auth =
    .title = Authentication
auth-status =
    .label = Status
    .checking = Checking authentication...
    .error = Authentication could not be checked.
    .logged-in-at = Logged In
    .logged-in = Logged In
    .unavailable = Authentication is unavailable in this environment.
    .logged-out = Logged Out
auth-detail =
    .checking = Checking authentication support...
    .error = Authentication could not be completed.
    .logged-in-session = Logged in {$method} at {$authenticated_at}.
        Session expires after {$expiration_hours} hours.
    .session-expires = Session expires after {$expiration_hours} hours.
    .web-passkey = Use your passkey to log in securely.
    .web-unavailable = Passkey sign-in is unavailable in this environment.
    .windows-passkey = Use Windows passkey verification to sign in securely.
    .windows-unavailable = Windows passkey verification is unavailable on this device.
    .native-unavailable = Real passkey sign-in is unavailable on this desktop target.
auth-button =
    .login = Log In
    .logout = Log Out
auth-provider =
    .label = Providers
    .passkey = Passkey
auth-method =
    .passkey = passkey
    .windows-passkey = Windows passkey
auth-prompt =
    .logged-in = You are now logged in.
    .logged-out = You are now logged out.
    .operation-failed = Authentication could not be completed.
auth-confirmation =
    .title = Are you sure?
    .description = You are about to log out...
    .confirm = Ok
    .cancel = Cancel
auth-message =
    .title = Message
    .ok = Ok
